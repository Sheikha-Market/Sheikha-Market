/**
 * ═══════════════════════════════════════════════════════════════════════════════
 *  📖 محرك الإعراب والتحليل النحوي المتقدم
 *  Advanced Arabic Parsing & Grammar Analysis Engine
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 *  بسم الله الرحمن الرحيم
 *  "وَعَلَّمَ آدَمَ الْأَسْمَاءَ كُلَّهَا" - البقرة: 31
 * 
 *  محرك متقدم لـ:
 *  - الإعراب التفصيلي لكل كلمة
 *  - تحليل السياق وفهم العلاقات
 *  - تحديد الفاعل والمفعول والعلاقات الإعرابية
 *  - نمذجة الجملة للاستخدام البرمجي
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 */

// ═══════════════════════════════════════════════════════════════════════════════
//  القواميس والقوائم اللغوية
// ═══════════════════════════════════════════════════════════════════════════════

// حروف الجر
const حروف_الجر = {
    'من': { المعنى: 'ابتداء الغاية أو التبعيض', نوع: 'أصلي' },
    'إلى': { المعنى: 'انتهاء الغاية', نوع: 'أصلي' },
    'عن': { المعنى: 'المجاوزة والبعد', نوع: 'أصلي' },
    'على': { المعنى: 'الاستعلاء', نوع: 'أصلي' },
    'في': { المعنى: 'الظرفية', نوع: 'أصلي' },
    'ب': { المعنى: 'الإلصاق أو السببية', نوع: 'أصلي' },
    'بـ': { المعنى: 'الإلصاق أو السببية', نوع: 'أصلي' },
    'ل': { المعنى: 'الملك أو الاختصاص', نوع: 'أصلي' },
    'لـ': { المعنى: 'الملك أو الاختصاص', نوع: 'أصلي' },
    'ك': { المعنى: 'التشبيه', نوع: 'أصلي' },
    'كـ': { المعنى: 'التشبيه', نوع: 'أصلي' },
    'حتى': { المعنى: 'انتهاء الغاية', نوع: 'أصلي' },
    'منذ': { المعنى: 'ابتداء الغاية الزمانية', نوع: 'أصلي' },
    'مذ': { المعنى: 'ابتداء الغاية الزمانية', نوع: 'أصلي' },
    'رب': { المعنى: 'التقليل أو التكثير', نوع: 'شبيه بالزائد' },
    'واو_القسم': { المعنى: 'القسم', نوع: 'للقسم' },
    'تاء_القسم': { المعنى: 'القسم', نوع: 'للقسم' },
    'باء_القسم': { المعنى: 'القسم', نوع: 'للقسم' }
};

// كان وأخواتها (الأفعال الناسخة)
const كان_وأخواتها = {
    'كان': { المعنى: 'اتصاف المبتدأ بالخبر في الزمن الماضي', يرفع: 'الاسم', ينصب: 'الخبر' },
    'أصبح': { المعنى: 'اتصاف المبتدأ بالخبر في وقت الصباح', يرفع: 'الاسم', ينصب: 'الخبر' },
    'أضحى': { المعنى: 'اتصاف المبتدأ بالخبر في وقت الضحى', يرفع: 'الاسم', ينصب: 'الخبر' },
    'أمسى': { المعنى: 'اتصاف المبتدأ بالخبر في وقت المساء', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ظل': { المعنى: 'استمرار الاتصاف نهاراً', يرفع: 'الاسم', ينصب: 'الخبر' },
    'بات': { المعنى: 'اتصاف المبتدأ بالخبر ليلاً', يرفع: 'الاسم', ينصب: 'الخبر' },
    'صار': { المعنى: 'التحول من حال إلى حال', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ليس': { المعنى: 'نفي اتصاف المبتدأ بالخبر', يرفع: 'الاسم', ينصب: 'الخبر' },
    'مازال': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ما زال': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'مابرح': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ما برح': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'مافتئ': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ما فتئ': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ماانفك': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ما انفك': { المعنى: 'الاستمرار', يرفع: 'الاسم', ينصب: 'الخبر' },
    'مادام': { المعنى: 'بيان المدة', يرفع: 'الاسم', ينصب: 'الخبر' },
    'ما دام': { المعنى: 'بيان المدة', يرفع: 'الاسم', ينصب: 'الخبر' }
};

// إن وأخواتها (الحروف الناسخة)
const إن_وأخواتها = {
    'إن': { المعنى: 'التوكيد', ينصب: 'الاسم', يرفع: 'الخبر' },
    'إنّ': { المعنى: 'التوكيد', ينصب: 'الاسم', يرفع: 'الخبر' },
    'أن': { المعنى: 'التوكيد (مفتوحة الهمزة)', ينصب: 'الاسم', يرفع: 'الخبر' },
    'أنّ': { المعنى: 'التوكيد (مفتوحة الهمزة)', ينصب: 'الاسم', يرفع: 'الخبر' },
    'كأن': { المعنى: 'التشبيه', ينصب: 'الاسم', يرفع: 'الخبر' },
    'كأنّ': { المعنى: 'التشبيه', ينصب: 'الاسم', يرفع: 'الخبر' },
    'لكن': { المعنى: 'الاستدراك', ينصب: 'الاسم', يرفع: 'الخبر' },
    'لكنّ': { المعنى: 'الاستدراك', ينصب: 'الاسم', يرفع: 'الخبر' },
    'ليت': { المعنى: 'التمني', ينصب: 'الاسم', يرفع: 'الخبر' },
    'لعل': { المعنى: 'الترجي', ينصب: 'الاسم', يرفع: 'الخبر' },
    'لعلّ': { المعنى: 'الترجي', ينصب: 'الاسم', يرفع: 'الخبر' }
};

// أدوات النصب
const أدوات_النصب = {
    'أن': { المعنى: 'المصدرية', ينصب: 'الفعل المضارع' },
    'لن': { المعنى: 'نفي المستقبل', ينصب: 'الفعل المضارع' },
    'كي': { المعنى: 'التعليل', ينصب: 'الفعل المضارع' },
    'لام_التعليل': { المعنى: 'التعليل', ينصب: 'الفعل المضارع' },
    'حتى': { المعنى: 'الغاية', ينصب: 'الفعل المضارع' },
    'فاء_السببية': { المعنى: 'السببية', ينصب: 'الفعل المضارع' },
    'واو_المعية': { المعنى: 'المعية', ينصب: 'الفعل المضارع' }
};

// أدوات الجزم
const أدوات_الجزم = {
    'لم': { المعنى: 'نفي الماضي', يجزم: 'الفعل المضارع' },
    'لما': { المعنى: 'نفي الماضي المستمر', يجزم: 'الفعل المضارع' },
    'لا': { المعنى: 'النهي (الناهية)', يجزم: 'الفعل المضارع' },
    'لام_الأمر': { المعنى: 'الأمر', يجزم: 'الفعل المضارع' }
};

// حروف العطف
const حروف_العطف = {
    'و': { المعنى: 'مطلق الجمع', الحكم: 'يُعطف على ما قبله' },
    'ف': { المعنى: 'الترتيب والتعقيب', الحكم: 'يُعطف على ما قبله' },
    'ثم': { المعنى: 'الترتيب والتراخي', الحكم: 'يُعطف على ما قبله' },
    'أو': { المعنى: 'التخيير أو الإباحة', الحكم: 'يُعطف على ما قبله' },
    'أم': { المعنى: 'المعادلة', الحكم: 'يُعطف على ما قبله' },
    'بل': { المعنى: 'الإضراب', الحكم: 'يُعطف على ما قبله' },
    'لا': { المعنى: 'النفي', الحكم: 'يُعطف على ما قبله' },
    'لكن': { المعنى: 'الاستدراك', الحكم: 'يُعطف على ما قبله' }
};

// أدوات الاستفهام
const أدوات_الاستفهام = ['هل', 'أ', 'ما', 'من', 'متى', 'أين', 'كيف', 'لماذا', 'كم', 'أي', 'أيّ'];

// أدوات النفي
const أدوات_النفي = ['لا', 'ما', 'لم', 'لن', 'ليس', 'لما', 'إن'];

// ظروف الزمان
const ظروف_الزمان = ['صباحاً', 'مساءً', 'ليلاً', 'نهاراً', 'غداً', 'أمس', 'الآن', 'حين', 'وقت', 'يوم', 'شهر', 'سنة', 'قبل', 'بعد', 'عند', 'إذ', 'إذا', 'متى', 'أيان', 'مذ', 'منذ'];

// ظروف المكان
const ظروف_المكان = ['فوق', 'تحت', 'أمام', 'خلف', 'يمين', 'يسار', 'وراء', 'قدام', 'بين', 'وسط', 'حول', 'عند', 'لدى', 'هنا', 'هناك', 'ثم', 'حيث', 'أين', 'أنى'];

// الضمائر
const الضمائر = {
    منفصلة: {
        رفع: ['أنا', 'نحن', 'أنتَ', 'أنتِ', 'أنتما', 'أنتم', 'أنتن', 'هو', 'هي', 'هما', 'هم', 'هن'],
        نصب: ['إياي', 'إيانا', 'إياكَ', 'إياكِ', 'إياكما', 'إياكم', 'إياكن', 'إياه', 'إياها', 'إياهما', 'إياهم', 'إياهن']
    },
    متصلة: ['ني', 'نا', 'كَ', 'كِ', 'كما', 'كم', 'كن', 'ه', 'ها', 'هما', 'هم', 'هن', 'ت', 'تُ', 'تَ', 'تِ', 'تما', 'تم', 'تن', 'ا', 'وا', 'ن']
};

// أسماء الإشارة
const أسماء_الإشارة = ['هذا', 'هذه', 'هذان', 'هاتان', 'هؤلاء', 'ذلك', 'تلك', 'ذانك', 'تانك', 'أولئك'];

// الأسماء الموصولة
const الأسماء_الموصولة = ['الذي', 'التي', 'اللذان', 'اللتان', 'الذين', 'اللاتي', 'اللائي', 'من', 'ما'];

// الأسماء الشائعة (أسماء أعلام وأسماء شائعة)
const الأسماء_الشائعة = [
    // أسماء أعلام مذكرة
    'محمد', 'أحمد', 'علي', 'عمر', 'خالد', 'سعد', 'فهد', 'سلمان', 'عبدالله', 'عبدالرحمن',
    'إبراهيم', 'يوسف', 'موسى', 'عيسى', 'نوح', 'آدم', 'داود', 'سليمان', 'زيد', 'عمرو',
    'طارق', 'حسن', 'حسين', 'جعفر', 'عثمان', 'معاذ', 'بلال', 'أنس', 'سعيد', 'مصطفى',
    // أسماء أعلام مؤنثة
    'فاطمة', 'عائشة', 'خديجة', 'مريم', 'زينب', 'أسماء', 'هند', 'سارة', 'نورة', 'شيخة',
    'ليلى', 'سلمى', 'هدى', 'رنا', 'دانة', 'لمى', 'منى', 'سناء', 'أمل', 'رحاب',
    // أسماء مواقع وأماكن
    'المدرسة', 'الجامعة', 'المسجد', 'البيت', 'المنزل', 'السوق', 'المكتبة', 'المستشفى',
    'الشركة', 'المصنع', 'المطار', 'الميناء', 'المتجر', 'المكتب', 'الحديقة', 'الملعب',
    // أسماء أشياء شائعة
    'الكتاب', 'القلم', 'الورقة', 'الدرس', 'العلم', 'المال', 'الطعام', 'الماء', 'السيارة',
    'الطالب', 'المعلم', 'الطبيب', 'المهندس', 'العامل', 'التاجر', 'الرجل', 'المرأة', 'الطفل',
    'الولد', 'البنت', 'الأب', 'الأم', 'الأخ', 'الأخت', 'العم', 'الخال', 'الجد', 'الجدة',
    // مصادر وأسماء معنوية
    'الحق', 'الصدق', 'العدل', 'الخير', 'الشر', 'الجمال', 'الحب', 'الكره', 'الفرح', 'الحزن',
    // أسماء معنوية بدون ال
    'حق', 'صدق', 'عدل', 'خير', 'شر', 'جمال', 'حب', 'كره', 'فرح', 'حزن',
    'نور', 'ظلام', 'علم', 'جهل', 'صبر', 'شكر', 'ذكر', 'فكر', 'عقل', 'قلب',
    'روح', 'نفس', 'سعادة', 'شقاء', 'نجاح', 'فشل', 'حياة', 'موت', 'إيمان', 'كفر',
    'توحيد', 'شرك', 'طاعة', 'معصية', 'خوف', 'رجاء', 'أمن', 'سلام', 'حرب', 'عهد'
];

// ═══════════════════════════════════════════════════════════════════════════════
//  فئة محرك الإعراب المتقدم
// ═══════════════════════════════════════════════════════════════════════════════

class محرك_الإعراب {
    constructor() {
        this.حروف_الجر = حروف_الجر;
        this.كان_وأخواتها = كان_وأخواتها;
        this.إن_وأخواتها = إن_وأخواتها;
        this.سجل_الإعراب = [];
    }

    // ─── إزالة التشكيل ───────────────────────────────────────────────────────────
    أزل_التشكيل(نص) {
        return نص.replace(/[\u064B-\u0652\u0670]/g, '');
    }

    // ─── تقسيم الجملة إلى كلمات ───────────────────────────────────────────────────
    قسّم_الجملة(جملة) {
        // تنظيف الجملة
        let نص = جملة.trim();
        
        // فصل الحروف المتصلة
        نص = نص.replace(/^و(?=[ا-ي])/g, 'و ');
        نص = نص.replace(/^ف(?=[ا-ي])/g, 'ف ');
        
        // تقسيم حسب المسافات
        const كلمات = نص.split(/\s+/).filter(ك => ك.length > 0);
        
        return كلمات;
    }

    // ─── تحديد نوع الكلمة ────────────────────────────────────────────────────────
    حدد_نوع_الكلمة(كلمة) {
        const ك = this.أزل_التشكيل(كلمة);
        
        // فحص حروف الجر
        if (this.حروف_الجر[ك] || this.حروف_الجر[كلمة]) {
            return { القسم: 'حرف', النوع: 'حرف جر', التفاصيل: this.حروف_الجر[ك] || this.حروف_الجر[كلمة] };
        }
        
        // فحص كان وأخواتها
        if (this.كان_وأخواتها[ك] || this.كان_وأخواتها[كلمة]) {
            return { القسم: 'فعل', النوع: 'فعل ناسخ', التفاصيل: this.كان_وأخواتها[ك] || this.كان_وأخواتها[كلمة] };
        }
        
        // فحص إن وأخواتها
        if (this.إن_وأخواتها[ك] || this.إن_وأخواتها[كلمة]) {
            return { القسم: 'حرف', النوع: 'حرف ناسخ', التفاصيل: this.إن_وأخواتها[ك] || this.إن_وأخواتها[كلمة] };
        }
        
        // فحص حروف العطف
        if (حروف_العطف[ك]) {
            return { القسم: 'حرف', النوع: 'حرف عطف', التفاصيل: حروف_العطف[ك] };
        }
        
        // فحص أدوات الاستفهام
        if (أدوات_الاستفهام.includes(ك)) {
            return { القسم: 'أداة', النوع: 'أداة استفهام' };
        }
        
        // فحص أدوات النفي
        if (أدوات_النفي.includes(ك)) {
            return { القسم: 'أداة', النوع: 'أداة نفي' };
        }
        
        // فحص الضمائر المنفصلة
        if (الضمائر.منفصلة.رفع.includes(ك)) {
            return { القسم: 'اسم', النوع: 'ضمير منفصل في محل رفع' };
        }
        if (الضمائر.منفصلة.نصب.includes(ك)) {
            return { القسم: 'اسم', النوع: 'ضمير منفصل في محل نصب' };
        }
        
        // فحص أسماء الإشارة
        if (أسماء_الإشارة.includes(ك)) {
            return { القسم: 'اسم', النوع: 'اسم إشارة' };
        }
        
        // فحص الأسماء الموصولة
        if (الأسماء_الموصولة.includes(ك)) {
            return { القسم: 'اسم', النوع: 'اسم موصول' };
        }
        
        // فحص ظروف الزمان
        if (ظروف_الزمان.includes(ك)) {
            return { القسم: 'اسم', النوع: 'ظرف زمان' };
        }
        
        // فحص ظروف المكان
        if (ظروف_المكان.includes(ك)) {
            return { القسم: 'اسم', النوع: 'ظرف مكان' };
        }
        
        // فحص الأسماء الشائعة (قبل فحص الأفعال)
        if (الأسماء_الشائعة.includes(ك) || الأسماء_الشائعة.includes(كلمة)) {
            return { القسم: 'اسم', النوع: 'اسم علم' };
        }
        
        // فحص الأسماء المعرفة بـ"ال" (قبل فحص الأفعال)
        if (ك.startsWith('ال') && ك.length > 3) {
            return { القسم: 'اسم', النوع: 'اسم معرف بأل' };
        }
        
        // فحص الفعل الماضي
        if (this.هل_فعل_ماضي(ك)) {
            return { القسم: 'فعل', النوع: 'فعل ماضي' };
        }
        
        // فحص الفعل المضارع
        if (this.هل_فعل_مضارع(ك)) {
            return { القسم: 'فعل', النوع: 'فعل مضارع' };
        }
        
        // فحص فعل الأمر
        if (this.هل_فعل_أمر(ك)) {
            return { القسم: 'فعل', النوع: 'فعل أمر' };
        }
        
        // افتراضياً: اسم
        return { القسم: 'اسم', النوع: 'اسم' };
    }

    // ─── فحص الفعل الماضي ────────────────────────────────────────────────────────
    هل_فعل_ماضي(كلمة) {
        const ك = this.أزل_التشكيل(كلمة);
        
        // الأفعال الشائعة
        const أفعال_ماضية = ['كتب', 'قرأ', 'ذهب', 'جاء', 'علم', 'فهم', 'سمع', 'رأى', 'قال', 'فعل', 
            'أكل', 'شرب', 'نام', 'قام', 'جلس', 'وقف', 'مشى', 'ركض', 'طار', 'سبح',
            'درس', 'عمل', 'لعب', 'ضحك', 'بكى', 'فرح', 'حزن', 'أحب', 'كره', 'عرف',
            'وجد', 'أخذ', 'أعطى', 'سأل', 'أجاب', 'فتح', 'أغلق', 'دخل', 'خرج', 'صعد',
            'نزل', 'وصل', 'رجع', 'بدأ', 'انتهى', 'استمر', 'تعلم', 'علّم', 'استغفر'];
        
        if (أفعال_ماضية.includes(ك)) return true;
        
        // نمط الثلاثي: فَعَلَ
        if (/^[ا-ي]{3}$/.test(ك)) return true;
        
        // نمط الرباعي المجرد: فَعْلَلَ
        if (/^[ا-ي]{4}$/.test(ك) && !ك.startsWith('ي') && !ك.startsWith('ت') && !ك.startsWith('ن') && !ك.startsWith('أ')) return true;
        
        // أفعل (مزيد بالهمزة)
        if (/^أ[ا-ي]{3}$/.test(ك)) return true;
        
        // تفعّل
        if (/^ت[ا-ي]{4}$/.test(ك) && !ك.startsWith('تُ')) return true;
        
        // انفعل
        if (/^ان[ا-ي]{3}$/.test(ك)) return true;
        
        // افتعل
        if (/^ا[ا-ي]ت[ا-ي]{2}$/.test(ك)) return true;
        
        // استفعل
        if (/^است[ا-ي]{4}$/.test(ك)) return true;
        
        return false;
    }

    // ─── فحص الفعل المضارع ────────────────────────────────────────────────────────
    هل_فعل_مضارع(كلمة) {
        const ك = this.أزل_التشكيل(كلمة);
        
        // يبدأ بأحد حروف المضارعة: ن، أ، ي، ت
        if (['ن', 'أ', 'ي', 'ت'].includes(ك[0]) && ك.length >= 3) {
            // استثناء بعض الكلمات
            const استثناءات = ['نحن', 'أنا', 'أنت', 'أنتم', 'أنتن', 'أنتما', 'يوم', 'تلك', 'يمين', 'يسار'];
            if (استثناءات.includes(ك)) return false;
            
            return true;
        }
        
        return false;
    }

    // ─── فحص فعل الأمر ────────────────────────────────────────────────────────────
    هل_فعل_أمر(كلمة) {
        const ك = this.أزل_التشكيل(كلمة);
        
        // أفعال أمر شائعة
        const أفعال_أمر = ['اكتب', 'اقرأ', 'اذهب', 'تعال', 'قل', 'كل', 'اشرب', 'نم', 'قم', 'اجلس',
            'قف', 'امش', 'اركض', 'ادرس', 'اعمل', 'العب', 'افتح', 'أغلق', 'ادخل', 'اخرج',
            'اصعد', 'انزل', 'ارجع', 'ابدأ', 'استمر', 'تعلم', 'استغفر', 'اسمع', 'انظر'];
        
        if (أفعال_أمر.includes(ك)) return true;
        
        // نمط الأمر من الثلاثي: افْعِلْ أو فُعْ
        if (/^ا[ا-ي]{3}$/.test(ك)) return true;
        
        return false;
    }

    // ─── تحديد علامة الإعراب ───────────────────────────────────────────────────────
    حدد_علامة_الإعراب(كلمة, الحالة) {
        const ك = this.أزل_التشكيل(كلمة);
        
        // التحقق من نوع الكلمة
        const هل_مثنى = ك.endsWith('ان') || ك.endsWith('ين');
        const هل_جمع_مذكر_سالم = ك.endsWith('ون') || ك.endsWith('ين');
        const هل_جمع_مؤنث_سالم = ك.endsWith('ات');
        const هل_أسماء_خمسة = ['أب', 'أخ', 'حم', 'فو', 'ذو'].some(اسم => ك.startsWith(اسم));
        const هل_أفعال_خمسة = this.هل_فعل_مضارع(ك) && (ك.endsWith('ون') || ك.endsWith('ان') || ك.endsWith('ين'));
        
        const علامات = {
            رفع: { أصلية: 'الضمة', الرمز: 'ُ' },
            نصب: { أصلية: 'الفتحة', الرمز: 'َ' },
            جر: { أصلية: 'الكسرة', الرمز: 'ِ' },
            جزم: { أصلية: 'السكون', الرمز: 'ْ' }
        };
        
        let النتيجة = علامات[الحالة];
        
        // العلامات الفرعية
        if (الحالة === 'رفع') {
            if (هل_مثنى) النتيجة = { فرعية: 'الألف', السبب: 'لأنه مثنى', الرمز: 'ا' };
            else if (هل_جمع_مذكر_سالم) النتيجة = { فرعية: 'الواو', السبب: 'لأنه جمع مذكر سالم', الرمز: 'و' };
            else if (هل_أسماء_خمسة) النتيجة = { فرعية: 'الواو', السبب: 'لأنه من الأسماء الخمسة', الرمز: 'و' };
            else if (هل_أفعال_خمسة) النتيجة = { فرعية: 'ثبوت النون', السبب: 'لأنه من الأفعال الخمسة' };
        } else if (الحالة === 'نصب') {
            if (هل_مثنى || هل_جمع_مذكر_سالم) النتيجة = { فرعية: 'الياء', السبب: 'لأنه مثنى أو جمع مذكر سالم', الرمز: 'ي' };
            else if (هل_جمع_مؤنث_سالم) النتيجة = { فرعية: 'الكسرة', السبب: 'لأنه جمع مؤنث سالم', الرمز: 'ِ' };
            else if (هل_أسماء_خمسة) النتيجة = { فرعية: 'الألف', السبب: 'لأنه من الأسماء الخمسة', الرمز: 'ا' };
            else if (هل_أفعال_خمسة) النتيجة = { فرعية: 'حذف النون', السبب: 'لأنه من الأفعال الخمسة' };
        } else if (الحالة === 'جر') {
            if (هل_مثنى || هل_جمع_مذكر_سالم) النتيجة = { فرعية: 'الياء', السبب: 'لأنه مثنى أو جمع مذكر سالم', الرمز: 'ي' };
            else if (هل_أسماء_خمسة) النتيجة = { فرعية: 'الياء', السبب: 'لأنه من الأسماء الخمسة', الرمز: 'ي' };
            // الممنوع من الصرف يُجر بالفتحة
        } else if (الحالة === 'جزم') {
            if (هل_أفعال_خمسة) النتيجة = { فرعية: 'حذف النون', السبب: 'لأنه من الأفعال الخمسة' };
            // الفعل المعتل يُجزم بحذف حرف العلة
        }
        
        return النتيجة;
    }

    // ═══════════════════════════════════════════════════════════════════════════════
    //  الإعراب الكامل للجملة
    // ═══════════════════════════════════════════════════════════════════════════════
    
    أعرب_الجملة(جملة) {
        const كلمات = this.قسّم_الجملة(جملة);
        const نوع_الجملة = this.حدد_نوع_الجملة(كلمات);
        
        const نتيجة = {
            الجملة_الأصلية: جملة,
            نوع_الجملة: نوع_الجملة,
            عدد_الكلمات: كلمات.length,
            التحليل: [],
            البنية: null,
            الملخص: ''
        };
        
        // تحليل بناءً على نوع الجملة
        if (نوع_الجملة.النوع === 'جملة فعلية') {
            نتيجة.التحليل = this.أعرب_جملة_فعلية(كلمات);
            نتيجة.البنية = this.بنية_جملة_فعلية(نتيجة.التحليل);
        } else if (نوع_الجملة.النوع === 'جملة اسمية') {
            نتيجة.التحليل = this.أعرب_جملة_اسمية(كلمات);
            نتيجة.البنية = this.بنية_جملة_اسمية(نتيجة.التحليل);
        } else if (نوع_الجملة.النوع === 'جملة منسوخة بكان') {
            نتيجة.التحليل = this.أعرب_جملة_كان(كلمات);
            نتيجة.البنية = this.بنية_جملة_كان(نتيجة.التحليل);
        } else if (نوع_الجملة.النوع === 'جملة منسوخة بإن') {
            نتيجة.التحليل = this.أعرب_جملة_إن(كلمات);
            نتيجة.البنية = this.بنية_جملة_إن(نتيجة.التحليل);
        } else {
            نتيجة.التحليل = this.أعرب_عام(كلمات);
        }
        
        // توليد الملخص
        نتيجة.الملخص = this.ولّد_ملخص(نتيجة);
        
        // حفظ في السجل
        this.سجل_الإعراب.push(نتيجة);
        
        return نتيجة;
    }

    // ─── تحديد نوع الجملة ────────────────────────────────────────────────────────
    حدد_نوع_الجملة(كلمات) {
        if (كلمات.length === 0) {
            return { النوع: 'جملة فارغة' };
        }
        
        const الكلمة_الأولى = this.أزل_التشكيل(كلمات[0]);
        const نوع_الأولى = this.حدد_نوع_الكلمة(كلمات[0]);
        
        // جملة منسوخة بكان
        if (this.كان_وأخواتها[الكلمة_الأولى] || this.كان_وأخواتها[كلمات[0]]) {
            return {
                النوع: 'جملة منسوخة بكان',
                الناسخ: كلمات[0],
                السبب: 'تبدأ بكان أو إحدى أخواتها'
            };
        }
        
        // جملة منسوخة بإن
        if (this.إن_وأخواتها[الكلمة_الأولى] || this.إن_وأخواتها[كلمات[0]]) {
            return {
                النوع: 'جملة منسوخة بإن',
                الناسخ: كلمات[0],
                السبب: 'تبدأ بإن أو إحدى أخواتها'
            };
        }
        
        // جملة فعلية
        if (نوع_الأولى.القسم === 'فعل') {
            return {
                النوع: 'جملة فعلية',
                السبب: 'تبدأ بفعل',
                نوع_الفعل: نوع_الأولى.النوع
            };
        }
        
        // جملة اسمية
        if (نوع_الأولى.القسم === 'اسم' || نوع_الأولى.القسم === 'أداة') {
            return {
                النوع: 'جملة اسمية',
                السبب: 'تبدأ باسم'
            };
        }
        
        return { النوع: 'غير محدد' };
    }

    // ─── إعراب الجملة الفعلية ────────────────────────────────────────────────────
    أعرب_جملة_فعلية(كلمات) {
        const تحليل = [];
        let فهرس_الفاعل = -1;
        let فهرس_المفعول = -1;
        let بعد_حرف_جر = false;
        let حرف_الجر_الحالي = null;
        
        for (let i = 0; i < كلمات.length; i++) {
            const كلمة = كلمات[i];
            const نوع = this.حدد_نوع_الكلمة(كلمة);
            
            let إعراب = {
                الكلمة: كلمة,
                الموقع: i + 1,
                ...نوع,
                الإعراب: '',
                العلامة: null,
                التعليل: ''
            };
            
            // الفعل (أول الجملة عادة)
            if (i === 0 && نوع.القسم === 'فعل') {
                if (نوع.النوع === 'فعل ماضي') {
                    إعراب.الإعراب = 'فعل ماضٍ مبني على الفتح';
                    إعراب.العلامة = { علامة: 'الفتحة', نوع: 'بناء' };
                    إعراب.التعليل = 'الفعل الماضي مبني دائماً';
                } else if (نوع.النوع === 'فعل مضارع') {
                    إعراب.الإعراب = 'فعل مضارع مرفوع';
                    إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'رفع');
                    إعراب.التعليل = 'الفعل المضارع مرفوع لأنه لم يسبقه ناصب أو جازم';
                } else if (نوع.النوع === 'فعل أمر') {
                    إعراب.الإعراب = 'فعل أمر مبني على السكون';
                    إعراب.العلامة = { علامة: 'السكون', نوع: 'بناء' };
                    إعراب.التعليل = 'فعل الأمر مبني دائماً';
                }
            }
            // حرف جر
            else if (نوع.النوع === 'حرف جر') {
                إعراب.الإعراب = 'حرف جر مبني على السكون لا محل له من الإعراب';
                إعراب.التعليل = 'الحروف مبنية لا محل لها من الإعراب';
                بعد_حرف_جر = true;
                حرف_الجر_الحالي = كلمة;
            }
            // حرف عطف
            else if (نوع.النوع === 'حرف عطف') {
                إعراب.الإعراب = 'حرف عطف مبني لا محل له من الإعراب';
                إعراب.التعليل = 'حروف العطف مبنية';
            }
            // اسم بعد حرف جر
            else if (بعد_حرف_جر && نوع.القسم === 'اسم') {
                إعراب.الإعراب = 'اسم مجرور بـ' + حرف_الجر_الحالي;
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'جر');
                إعراب.التعليل = 'الاسم بعد حرف الجر يكون مجروراً';
                إعراب.العلاقة = { نوع: 'جر', السبب: حرف_الجر_الحالي };
                بعد_حرف_جر = false;
            }
            // الفاعل (أول اسم بعد الفعل)
            else if (نوع.القسم === 'اسم' && فهرس_الفاعل === -1 && i > 0) {
                فهرس_الفاعل = i;
                إعراب.الإعراب = 'فاعل مرفوع';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'رفع');
                إعراب.التعليل = 'الفاعل يأتي بعد الفعل ويكون مرفوعاً';
                إعراب.الدور = 'فاعل';
            }
            // المفعول به (اسم بعد الفاعل)
            else if (نوع.القسم === 'اسم' && فهرس_الفاعل !== -1 && فهرس_المفعول === -1) {
                فهرس_المفعول = i;
                إعراب.الإعراب = 'مفعول به منصوب';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'نصب');
                إعراب.التعليل = 'المفعول به يقع عليه فعل الفاعل ويكون منصوباً';
                إعراب.الدور = 'مفعول به';
            }
            // ظرف
            else if (نوع.النوع === 'ظرف زمان' || نوع.النوع === 'ظرف مكان') {
                إعراب.الإعراب = نوع.النوع + ' منصوب على الظرفية';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'نصب');
                إعراب.التعليل = 'الظروف منصوبة على الظرفية';
                إعراب.الدور = 'مفعول فيه';
            }
            // اسم إضافي
            else if (نوع.القسم === 'اسم') {
                إعراب.الإعراب = 'اسم يحتاج تحليل السياق';
                إعراب.التعليل = 'يحدد إعرابه حسب موقعه ووظيفته في الجملة';
            }
            
            تحليل.push(إعراب);
        }
        
        return تحليل;
    }

    // ─── إعراب الجملة الاسمية ────────────────────────────────────────────────────
    أعرب_جملة_اسمية(كلمات) {
        const تحليل = [];
        let فهرس_المبتدأ = -1;
        let فهرس_الخبر = -1;
        let بعد_حرف_جر = false;
        let حرف_الجر_الحالي = null;
        
        for (let i = 0; i < كلمات.length; i++) {
            const كلمة = كلمات[i];
            const نوع = this.حدد_نوع_الكلمة(كلمة);
            
            let إعراب = {
                الكلمة: كلمة,
                الموقع: i + 1,
                ...نوع,
                الإعراب: '',
                العلامة: null,
                التعليل: ''
            };
            
            // حرف جر
            if (نوع.النوع === 'حرف جر') {
                إعراب.الإعراب = 'حرف جر مبني لا محل له من الإعراب';
                بعد_حرف_جر = true;
                حرف_الجر_الحالي = كلمة;
            }
            // اسم بعد حرف جر
            else if (بعد_حرف_جر && نوع.القسم === 'اسم') {
                إعراب.الإعراب = 'اسم مجرور بـ' + حرف_الجر_الحالي;
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'جر');
                إعراب.التعليل = 'الاسم بعد حرف الجر مجرور';
                إعراب.العلاقة = { نوع: 'جر', السبب: حرف_الجر_الحالي };
                
                // شبه الجملة قد تكون خبراً
                if (فهرس_المبتدأ !== -1 && فهرس_الخبر === -1) {
                    إعراب.الإعراب += ' - والجار والمجرور متعلقان بمحذوف خبر';
                    فهرس_الخبر = i;
                }
                بعد_حرف_جر = false;
            }
            // المبتدأ (أول اسم)
            else if (نوع.القسم === 'اسم' && فهرس_المبتدأ === -1) {
                فهرس_المبتدأ = i;
                إعراب.الإعراب = 'مبتدأ مرفوع';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'رفع');
                إعراب.التعليل = 'المبتدأ اسم مرفوع يقع في أول الجملة الاسمية';
                إعراب.الدور = 'مبتدأ';
            }
            // الخبر (اسم بعد المبتدأ)
            else if (نوع.القسم === 'اسم' && فهرس_المبتدأ !== -1 && فهرس_الخبر === -1) {
                فهرس_الخبر = i;
                إعراب.الإعراب = 'خبر مرفوع';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'رفع');
                إعراب.التعليل = 'الخبر يتم به معنى المبتدأ ويكون مرفوعاً';
                إعراب.الدور = 'خبر';
            }
            // فعل في الجملة الاسمية (خبر جملة فعلية)
            else if (نوع.القسم === 'فعل' && فهرس_المبتدأ !== -1 && فهرس_الخبر === -1) {
                فهرس_الخبر = i;
                إعراب.الإعراب = 'فعل - والجملة الفعلية في محل رفع خبر المبتدأ';
                إعراب.التعليل = 'الجملة الفعلية بعد المبتدأ تكون خبراً له';
                إعراب.الدور = 'خبر (جملة فعلية)';
            }
            
            تحليل.push(إعراب);
        }
        
        return تحليل;
    }

    // ─── إعراب جملة كان وأخواتها ────────────────────────────────────────────────
    أعرب_جملة_كان(كلمات) {
        const تحليل = [];
        let فهرس_الاسم = -1;
        let فهرس_الخبر = -1;
        
        for (let i = 0; i < كلمات.length; i++) {
            const كلمة = كلمات[i];
            const نوع = this.حدد_نوع_الكلمة(كلمة);
            
            let إعراب = {
                الكلمة: كلمة,
                الموقع: i + 1,
                ...نوع,
                الإعراب: '',
                العلامة: null,
                التعليل: ''
            };
            
            // كان وأخواتها
            if (i === 0 && نوع.النوع === 'فعل ناسخ') {
                إعراب.الإعراب = 'فعل ماضٍ ناقص مبني على الفتح';
                إعراب.التعليل = 'كان وأخواتها أفعال ناسخة ترفع المبتدأ اسماً لها وتنصب الخبر خبراً لها';
            }
            // اسم كان (مرفوع)
            else if (نوع.القسم === 'اسم' && فهرس_الاسم === -1 && i > 0) {
                فهرس_الاسم = i;
                إعراب.الإعراب = 'اسم ' + كلمات[0] + ' مرفوع';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'رفع');
                إعراب.التعليل = 'اسم كان وأخواتها يكون مرفوعاً';
                إعراب.الدور = 'اسم كان';
            }
            // خبر كان (منصوب)
            else if (نوع.القسم === 'اسم' && فهرس_الاسم !== -1 && فهرس_الخبر === -1) {
                فهرس_الخبر = i;
                إعراب.الإعراب = 'خبر ' + كلمات[0] + ' منصوب';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'نصب');
                إعراب.التعليل = 'خبر كان وأخواتها يكون منصوباً';
                إعراب.الدور = 'خبر كان';
            }
            
            تحليل.push(إعراب);
        }
        
        return تحليل;
    }

    // ─── إعراب جملة إن وأخواتها ────────────────────────────────────────────────
    أعرب_جملة_إن(كلمات) {
        const تحليل = [];
        let فهرس_الاسم = -1;
        let فهرس_الخبر = -1;
        
        for (let i = 0; i < كلمات.length; i++) {
            const كلمة = كلمات[i];
            let نوع = this.حدد_نوع_الكلمة(كلمة);
            
            let إعراب = {
                الكلمة: كلمة,
                الموقع: i + 1,
                ...نوع,
                الإعراب: '',
                العلامة: null,
                التعليل: ''
            };
            
            // إن وأخواتها
            if (i === 0 && نوع.النوع === 'حرف ناسخ') {
                إعراب.الإعراب = 'حرف توكيد ونصب مبني لا محل له من الإعراب';
                إعراب.التعليل = 'إن وأخواتها حروف ناسخة تنصب المبتدأ اسماً لها وترفع الخبر خبراً لها';
            }
            // اسم إن (منصوب)
            else if (نوع.القسم === 'اسم' && فهرس_الاسم === -1 && i > 0) {
                فهرس_الاسم = i;
                إعراب.الإعراب = 'اسم ' + كلمات[0] + ' منصوب';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'نصب');
                إعراب.التعليل = 'اسم إن وأخواتها يكون منصوباً';
                إعراب.الدور = 'اسم إن';
            }
            // خبر إن (مرفوع) - يقبل أي كلمة بعد اسم إن كخبر
            else if (فهرس_الاسم !== -1 && فهرس_الخبر === -1) {
                فهرس_الخبر = i;
                // تصحيح نوع الكلمة إلى اسم إذا كانت في موقع الخبر
                إعراب.القسم = 'اسم';
                إعراب.النوع = نوع.القسم === 'فعل' ? 'اسم (في موقع الخبر)' : نوع.النوع;
                إعراب.الإعراب = 'خبر ' + كلمات[0] + ' مرفوع';
                إعراب.العلامة = this.حدد_علامة_الإعراب(كلمة, 'رفع');
                إعراب.التعليل = 'خبر إن وأخواتها يكون مرفوعاً';
                إعراب.الدور = 'خبر إن';
            }
            
            تحليل.push(إعراب);
        }
        
        return تحليل;
    }

    // ─── إعراب عام ────────────────────────────────────────────────────────────────
    أعرب_عام(كلمات) {
        const تحليل = [];
        
        for (let i = 0; i < كلمات.length; i++) {
            const كلمة = كلمات[i];
            const نوع = this.حدد_نوع_الكلمة(كلمة);
            
            تحليل.push({
                الكلمة: كلمة,
                الموقع: i + 1,
                ...نوع,
                الإعراب: 'يحتاج تحليل السياق',
                التعليل: 'يحدد إعرابه حسب موقعه في الجملة'
            });
        }
        
        return تحليل;
    }

    // ─── بناء البنية للجملة الفعلية ───────────────────────────────────────────────
    بنية_جملة_فعلية(تحليل) {
        const بنية = {
            الفعل: null,
            الفاعل: null,
            المفعول_به: null,
            المجرورات: [],
            الظروف: [],
            أخرى: []
        };
        
        for (const عنصر of تحليل) {
            if (عنصر.القسم === 'فعل' && !بنية.الفعل) {
                بنية.الفعل = عنصر;
            } else if (عنصر.الدور === 'فاعل') {
                بنية.الفاعل = عنصر;
            } else if (عنصر.الدور === 'مفعول به') {
                بنية.المفعول_به = عنصر;
            } else if (عنصر.العلاقة?.نوع === 'جر') {
                بنية.المجرورات.push(عنصر);
            } else if (عنصر.الدور === 'مفعول فيه') {
                بنية.الظروف.push(عنصر);
            } else if (عنصر.النوع !== 'حرف جر' && عنصر.النوع !== 'حرف عطف') {
                بنية.أخرى.push(عنصر);
            }
        }
        
        return بنية;
    }

    // ─── بناء البنية للجملة الاسمية ───────────────────────────────────────────────
    بنية_جملة_اسمية(تحليل) {
        const بنية = {
            المبتدأ: null,
            الخبر: null,
            المجرورات: [],
            أخرى: []
        };
        
        for (const عنصر of تحليل) {
            if (عنصر.الدور === 'مبتدأ') {
                بنية.المبتدأ = عنصر;
            } else if (عنصر.الدور === 'خبر' || عنصر.الدور === 'خبر (جملة فعلية)') {
                بنية.الخبر = عنصر;
            } else if (عنصر.العلاقة?.نوع === 'جر') {
                بنية.المجرورات.push(عنصر);
            } else {
                بنية.أخرى.push(عنصر);
            }
        }
        
        return بنية;
    }

    // ─── بناء البنية لجملة كان ───────────────────────────────────────────────────
    بنية_جملة_كان(تحليل) {
        const بنية = {
            الناسخ: null,
            الاسم: null,
            الخبر: null,
            أخرى: []
        };
        
        for (const عنصر of تحليل) {
            if (عنصر.النوع === 'فعل ناسخ') {
                بنية.الناسخ = عنصر;
            } else if (عنصر.الدور === 'اسم كان') {
                بنية.الاسم = عنصر;
            } else if (عنصر.الدور === 'خبر كان') {
                بنية.الخبر = عنصر;
            } else {
                بنية.أخرى.push(عنصر);
            }
        }
        
        return بنية;
    }

    // ─── بناء البنية لجملة إن ───────────────────────────────────────────────────
    بنية_جملة_إن(تحليل) {
        const بنية = {
            الناسخ: null,
            الاسم: null,
            الخبر: null,
            أخرى: []
        };
        
        for (const عنصر of تحليل) {
            if (عنصر.النوع === 'حرف ناسخ') {
                بنية.الناسخ = عنصر;
            } else if (عنصر.الدور === 'اسم إن') {
                بنية.الاسم = عنصر;
            } else if (عنصر.الدور === 'خبر إن') {
                بنية.الخبر = عنصر;
            } else {
                بنية.أخرى.push(عنصر);
            }
        }
        
        return بنية;
    }

    // ─── توليد ملخص ────────────────────────────────────────────────────────────────
    ولّد_ملخص(نتيجة) {
        let ملخص = `📝 الجملة: "${نتيجة.الجملة_الأصلية}"\n`;
        ملخص += `📊 النوع: ${نتيجة.نوع_الجملة.النوع}\n`;
        ملخص += `📐 عدد الكلمات: ${نتيجة.عدد_الكلمات}\n\n`;
        ملخص += `📖 الإعراب التفصيلي:\n`;
        ملخص += `${'─'.repeat(50)}\n`;
        
        for (const عنصر of نتيجة.التحليل) {
            ملخص += `\n${عنصر.الموقع}. ${عنصر.الكلمة}\n`;
            ملخص += `   • القسم: ${عنصر.القسم}\n`;
            ملخص += `   • النوع: ${عنصر.النوع}\n`;
            ملخص += `   • الإعراب: ${عنصر.الإعراب}\n`;
            if (عنصر.العلامة) {
                const علامة = عنصر.العلامة.أصلية || عنصر.العلامة.فرعية || عنصر.العلامة.علامة;
                ملخص += `   • العلامة: ${علامة}`;
                if (عنصر.العلامة.السبب) ملخص += ` (${عنصر.العلامة.السبب})`;
                ملخص += '\n';
            }
            ملخص += `   • التعليل: ${عنصر.التعليل}\n`;
        }
        
        return ملخص;
    }

    // ═══════════════════════════════════════════════════════════════════════════════
    //  نمذجة الجملة للاستخدام البرمجي
    // ═══════════════════════════════════════════════════════════════════════════════
    
    نمذج_للبرمجة(جملة) {
        const تحليل = this.أعرب_الجملة(جملة);
        
        const نموذج = {
            // البنية الأساسية
            جملة: جملة,
            نوع: تحليل.نوع_الجملة.النوع,
            
            // العناصر الرئيسية
            عناصر: تحليل.التحليل.map(ع => ({
                كلمة: ع.الكلمة,
                قسم: ع.القسم,
                نوع: ع.النوع,
                دور: ع.الدور || null,
                إعراب: ع.الإعراب,
                علامة: ع.العلامة
            })),
            
            // البنية المنطقية
            بنية: تحليل.البنية,
            
            // للاستخدام في السكربتات
            toJSON: function() {
                return JSON.stringify(this, null, 2);
            },
            
            // استخراج الفاعل
            الفاعل: function() {
                return this.عناصر.find(ع => ع.دور === 'فاعل' || ع.دور === 'مبتدأ');
            },
            
            // استخراج المفعول
            المفعول: function() {
                return this.عناصر.find(ع => ع.دور === 'مفعول به');
            },
            
            // استخراج الفعل
            الفعل: function() {
                return this.عناصر.find(ع => ع.قسم === 'فعل');
            },
            
            // التحقق من النوع
            هل_فعلية: function() {
                return this.نوع === 'جملة فعلية';
            },
            
            هل_اسمية: function() {
                return this.نوع === 'جملة اسمية';
            }
        };
        
        return نموذج;
    }
}

// ═══════════════════════════════════════════════════════════════════════════════
//  التصدير
// ═══════════════════════════════════════════════════════════════════════════════

const محرك = new محرك_الإعراب();

module.exports = {
    محرك_الإعراب,
    محرك,
    حروف_الجر,
    كان_وأخواتها,
    إن_وأخواتها,
    أدوات_النصب,
    أدوات_الجزم,
    حروف_العطف,
    ظروف_الزمان,
    ظروف_المكان,
    الضمائر,
    أسماء_الإشارة,
    الأسماء_الموصولة,
    الأسماء_الشائعة
};

console.log('✅ محرك الإعراب والتحليل النحوي المتقدم - تم التحميل');
